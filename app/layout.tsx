import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Raman's Portfolio",
  description: "the portfolio of raman, a video editor and motion designer specializing in YouTube content, social media assets, and product demos. Explore a curated selection of work showcasing expertise in long-form edits, dynamic motion graphics, and sleek SaaS presentations. With a focus on storytelling and visual impact, this portfolio highlights the ability to transform raw footage into engaging narratives that captivate audiences across platforms.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
